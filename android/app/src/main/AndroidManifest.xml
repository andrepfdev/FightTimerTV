<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Necessárias para ler o IP/SSID da rede Wi-Fi e montar a URL/QR code da TV -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Foreground service que mantém o cronômetro/servidor HTTP vivos
         quando o app é minimizado de propósito durante uma luta (ver
         TimerForegroundService.kt). "connectedDevice" (usado numa
         primeira tentativa) tem pré-requisitos de Bluetooth/companion
         device no Android 14+ que este app não atende e derrubava o app
         inteiro com uma exceção não tratada ao chamar startForeground()
         — confirmado em teste real (crash ao apertar "INICIAR").
         "specialUse" é o tipo genérico documentado pra casos que não se
         encaixam nos tipos específicos, sem essas exigências. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:screenOrientation="fullSensor"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>
      <service
        android:name=".TimerForegroundService"
        android:exported="false"
        android:foregroundServiceType="specialUse">
        <property
          android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
          android:value="Mantém o cronômetro e o servidor HTTP local transmitindo para a TV enquanto o app está minimizado" />
      </service>
    </application>
</manifest>
